.ui.stackable.grid
  {% capture 'site_logo_url' %}
  {% editable_file 'site_logo', hint: 'Site-wide logo displayed in the page footer.' %}
  {% endeditable_file %}
  {% endcapture %}
  #logo.center.aligned.four.wide.column
    %a{ :href => '/' }
      %img.ui.center.aligned.image{ :src => '{% if site_logo_url != "" %}{{ site_logo_url | resize: "250x150#" | escape }}{% else %}{{ "logo_placeholder.png" | theme_image_url | escape }}{% endif %}', :alt => '{{ site.name | escape }}' }

  #site-nav.two.wide.column
    .ui.link.list
      {% with_scope depth: 1 %}
      {% for p in site.pages %}
      %a.item{ :href => '{% path_to p %}' } {{ p.title | escape }}
      {% endfor %}
      {% endwith_scope %}

  #site-links.two.wide.column
    .ui.link.list
      {% for link in contents.site_links %}
      %a.item{ :href => '{{ link.url | escape }}' } {{ link.name | escape }}
      {% endfor %}
